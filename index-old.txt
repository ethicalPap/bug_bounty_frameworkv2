<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bug Bounty Platform</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Courier New', 'Monaco', 'Menlo', monospace;
            background-color: #000000;
            color: #00ff00;
            height: 100vh;
            overflow: hidden;
            background-image: 
                radial-gradient(circle at 25% 25%, #001100 0%, transparent 50%),
                radial-gradient(circle at 75% 75%, #001100 0%, transparent 50%);
        }

        /* Layout */
        .container {
            display: flex;
            height: 100vh;
            border: 2px solid #00ff00;
        }

        /* Sidebar */
        .sidebar {
            width: 280px;
            background-color: #0a0a0a;
            border-right: 2px solid #00ff00;
            display: flex;
            flex-direction: column;
            position: relative;
        }

        .sidebar::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: repeating-linear-gradient(
                90deg,
                transparent,
                transparent 2px,
                rgba(0, 255, 0, 0.03) 2px,
                rgba(0, 255, 0, 0.03) 4px
            );
            pointer-events: none;
        }

        .logo {
            padding: 20px;
            border-bottom: 2px solid #00ff00;
            font-size: 20px;
            font-weight: bold;
            color: #00ff00;
            text-align: center;
            background-color: #000000;
            text-transform: uppercase;
            letter-spacing: 2px;
        }

        .nav-item {
            padding: 15px 20px;
            cursor: pointer;
            border-bottom: 1px solid #003300;
            transition: all 0.1s ease;
            display: flex;
            align-items: center;
            gap: 10px;
            background-color: #000000;
            color: #00cc00;
            text-transform: uppercase;
            font-size: 14px;
            letter-spacing: 1px;
            border-left: 3px solid transparent;
        }

        .nav-item:hover {
            background-color: #001100;
            border-left: 3px solid #00ff00;
            color: #00ff00;
            box-shadow: inset 0 0 10px rgba(0, 255, 0, 0.1);
        }

        .nav-item.active {
            background-color: #002200;
            border-left: 3px solid #00ff00;
            color: #00ff00;
            box-shadow: inset 0 0 15px rgba(0, 255, 0, 0.2);
        }

        .user-info {
            margin-top: auto;
            padding: 20px;
            border-top: 2px solid #00ff00;
            background-color: #000000;
        }

        /* Main Content */
        .main {
            flex: 1;
            display: flex;
            flex-direction: column;
            overflow: hidden;
            background-color: #000000;
        }

        .header {
            padding: 20px 30px;
            border-bottom: 2px solid #00ff00;
            background-color: #000000;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .content {
            flex: 1;
            padding: 20px 30px;
            overflow-y: auto;
            background-color: #000000;
        }

        /* Auth Form */
        .auth-container {
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100vh;
            background-color: #000000;
            background-image: 
                radial-gradient(circle at 20% 80%, #001100 0%, transparent 50%),
                radial-gradient(circle at 80% 20%, #001100 0%, transparent 50%);
        }

        .auth-form {
            background-color: #0a0a0a;
            border: 2px solid #00ff00;
            padding: 40px;
            width: 400px;
            box-shadow: 0 0 20px rgba(0, 255, 0, 0.3);
        }

        .auth-title {
            text-align: center;
            font-size: 26px;
            margin-bottom: 30px;
            color: #00ff00;
            text-transform: uppercase;
            letter-spacing: 3px;
        }

        /* Forms */
        .form-group {
            margin-bottom: 20px;
        }

        .form-group label {
            display: block;
            margin-bottom: 8px;
            color: #00cc00;
            font-weight: bold;
            text-transform: uppercase;
            font-size: 13px;
            letter-spacing: 1px;
        }

        .form-group input, .form-group select, .form-group textarea {
            width: 100%;
            padding: 12px 15px;
            border: 2px solid #003300;
            background-color: #000000;
            color: #00ff00;
            font-size: 16px;
            font-family: 'Courier New', monospace;
            transition: border-color 0.2s ease;
        }

        .form-group input:focus, .form-group select:focus, .form-group textarea:focus {
            outline: none;
            border-color: #00ff00;
            box-shadow: 0 0 10px rgba(0, 255, 0, 0.3);
        }

        /* Buttons */
        .btn {
            padding: 12px 20px;
            border: 2px solid #00ff00;
            background-color: #000000;
            color: #00ff00;
            cursor: pointer;
            font-size: 14px;
            font-weight: bold;
            transition: all 0.2s ease;
            display: inline-flex;
            align-items: center;
            gap: 8px;
            text-transform: uppercase;
            letter-spacing: 1px;
            font-family: 'Courier New', monospace;
        }

        .btn-primary {
            background-color: #001100;
            border-color: #00ff00;
        }

        .btn-primary:hover {
            background-color: #00ff00;
            color: #000000;
            box-shadow: 0 0 15px rgba(0, 255, 0, 0.5);
        }

        .btn-secondary {
            background-color: #000000;
            border-color: #006600;
            color: #006600;
        }

        .btn-secondary:hover {
            background-color: #006600;
            color: #000000;
        }

        .btn-success {
            background-color: #000000;
            border-color: #00aa00;
            color: #00aa00;
        }

        .btn-danger {
            background-color: #000000;
            border-color: #ff0000;
            color: #ff0000;
        }

        .btn-danger:hover {
            background-color: #ff0000;
            color: #000000;
        }

        .btn-small {
            padding: 8px 12px;
            font-size: 12px;
        }

        /* Tables */
        .table-container {
            background-color: #000000;
            border: 2px solid #00ff00;
            overflow: hidden;
        }

        .table {
            width: 100%;
            border-collapse: collapse;
            font-family: 'Courier New', monospace;
        }

        .table th, .table td {
            padding: 12px 15px;
            text-align: left;
            border-bottom: 1px solid #003300;
            font-size: 14px;
        }

        .table th {
            background-color: #001100;
            color: #00ff00;
            font-weight: bold;
            text-transform: uppercase;
            letter-spacing: 1px;
            border-bottom: 2px solid #00ff00;
        }

        .table tbody tr:hover {
            background-color: #001100;
        }

        .table tbody tr:nth-child(even) {
            background-color: #0a0a0a;
        }

        /* Cards */
        .card {
            background-color: #0a0a0a;
            border: 2px solid #00ff00;
            padding: 20px;
            margin-bottom: 20px;
            position: relative;
        }

        .card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 2px;
            background: linear-gradient(90deg, transparent, #00ff00, transparent);
        }

        .card-title {
            font-size: 18px;
            font-weight: bold;
            margin-bottom: 15px;
            color: #00ff00;
            text-transform: uppercase;
            letter-spacing: 2px;
            border-bottom: 1px solid #003300;
            padding-bottom: 10px;
        }

        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 20px;
            margin-bottom: 20px;
        }

        .stat-card {
            background-color: #000000;
            border: 2px solid #003300;
            padding: 20px;
            text-align: center;
            position: relative;
        }

        .stat-card::after {
            content: '';
            position: absolute;
            top: 5px;
            right: 5px;
            width: 10px;
            height: 10px;
            border: 2px solid #00ff00;
            background-color: #00ff00;
        }

        .stat-value {
            font-size: 30px;
            font-weight: bold;
            margin-bottom: 8px;
            color: #00ff00;
            font-family: 'Courier New', monospace;
        }

        .stat-label {
            color: #006600;
            font-size: 13px;
            font-weight: bold;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        /* Status badges */
        .status {
            padding: 4px 8px;
            border: 1px solid;
            font-size: 12px;
            font-weight: bold;
            text-transform: uppercase;
            letter-spacing: 1px;
            font-family: 'Courier New', monospace;
            background-color: #000000;
        }

        .status-completed {
            border-color: #00ff00;
            color: #00ff00;
        }

        .status-running {
            border-color: #ffff00;
            color: #ffff00;
        }

        .status-pending {
            border-color: #ff8800;
            color: #ff8800;
        }

        .status-failed {
            border-color: #ff0000;
            color: #ff0000;
        }

        .status-active {
            border-color: #00ff00;
            color: #00ff00;
        }

        .status-inactive {
            border-color: #666666;
            color: #666666;
        }

        /* Severity badges */
        .severity-critical {
            border-color: #ff0000;
            color: #ff0000;
            background-color: #330000;
        }

        .severity-high {
            border-color: #ff4400;
            color: #ff4400;
            background-color: #331100;
        }

        .severity-medium {
            border-color: #ff8800;
            color: #ff8800;
            background-color: #332200;
        }

        .severity-low {
            border-color: #ffff00;
            color: #ffff00;
            background-color: #333300;
        }

        .severity-info {
            border-color: #0088ff;
            color: #0088ff;
            background-color: #001133;
        }

        /* Filters */
        .filters {
            display: flex;
            gap: 15px;
            margin-bottom: 20px;
            flex-wrap: wrap;
            align-items: center;
            padding: 15px;
            border: 1px solid #003300;
            background-color: #0a0a0a;
        }

        .filter-group {
            display: flex;
            flex-direction: column;
            gap: 5px;
        }

        .filter-group label {
            font-size: 12px;
            color: #00cc00;
            font-weight: bold;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        .filter-group select, .filter-group input {
            padding: 8px 10px;
            border: 1px solid #003300;
            background-color: #000000;
            color: #00ff00;
            font-size: 14px;
            font-family: 'Courier New', monospace;
        }

        .filter-group select:focus, .filter-group input:focus {
            outline: none;
            border-color: #00ff00;
        }

        .scan-info {
            background-color: #001100;
            border: 2px solid #00ff00;
            padding: 15px;
            margin-bottom: 20px;
            color: #00cc00;
            font-family: 'Courier New', monospace;
        }

        .scan-info h4 {
            color: #00ff00;
            margin-bottom: 8px;
            text-transform: uppercase;
            letter-spacing: 2px;
        }

        /* Utilities */
        .hidden {
            display: none;
        }

        .text-center {
            text-align: center;
        }

        .mb-4 {
            margin-bottom: 16px;
        }

        .mr-2 {
            margin-right: 8px;
        }

        .loading {
            opacity: 0.6;
            pointer-events: none;
        }

        /* Error and success messages */
        .message {
            padding: 12px 15px;
            border: 2px solid;
            margin-bottom: 15px;
            font-weight: bold;
            font-family: 'Courier New', monospace;
            background-color: #000000;
        }

        .message-error {
            border-color: #ff0000;
            color: #ff0000;
        }

        .message-success {
            border-color: #00ff00;
            color: #00ff00;
        }

        .message-warning {
            border-color: #ffff00;
            color: #ffff00;
        }

        /* Loading spinner */
        .spinner {
            border: 2px solid #003300;
            border-top: 2px solid #00ff00;
            width: 20px;
            height: 20px;
            animation: spin 1s linear infinite;
            display: inline-block;
            margin-right: 8px;
        }

        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }

        /* Header styling */
        #page-title {
            color: #00ff00;
            text-transform: uppercase;
            letter-spacing: 3px;
            font-family: 'Courier New', monospace;
        }

        #global-target-filter {
            padding: 8px 12px;
            border: 2px solid #003300;
            background-color: #000000;
            color: #00ff00;
            font-family: 'Courier New', monospace;
        }

        #global-target-filter:focus {
            outline: none;
            border-color: #00ff00;
        }

        /* User info styling */
        #user-name {
            color: #00cc00;
            font-weight: bold;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        /* Target stats styling */
        .target-stat {
            background-color: #000000;
            border: 2px solid #003300;
            padding: 15px;
            position: relative;
        }

        .target-stat::before {
            content: '> ';
            color: #00ff00;
            font-weight: bold;
        }

        /* Table link styling */
        .table a {
            color: #00ff00;
            text-decoration: none;
        }

        .table a:hover {
            color: #ffffff;
            text-decoration: underline;
        }

        /* Responsive */
        @media (max-width: 768px) {
            .sidebar {
                width: 250px;
            }
            
            .content {
                padding: 15px;
            }
            
            .filters {
                flex-direction: column;
                align-items: stretch;
            }
            
            .stats-grid {
                grid-template-columns: 1fr;
            }
        }
    </style>
</head>
<body>
    <!-- Authentication Form -->
    <div id="auth-container" class="auth-container">
        <div class="auth-form">
            <h2 class="auth-title">🛡️ Bug Bounty Platform</h2>
            <div id="auth-messages"></div>
            <form id="auth-form">
                <div class="form-group">
                    <label for="email">Email</label>
                    <input type="email" id="email" required>
                </div>
                <div class="form-group">
                    <label for="password">Password</label>
                    <input type="password" id="password" required>
                </div>
                <div id="register-fields" class="hidden">
                    <div class="form-group">
                        <label for="fullName">Full Name</label>
                        <input type="text" id="fullName">
                    </div>
                    <div class="form-group">
                        <label for="organizationName">Organization Name</label>
                        <input type="text" id="organizationName">
                    </div>
                </div>
                <div class="form-group">
                    <button type="submit" class="btn btn-primary" style="width: 100%;" id="auth-submit-btn">Login</button>
                </div>
                <div class="text-center">
                    <button type="button" id="toggle-auth" class="btn btn-secondary">Register</button>
                </div>
            </form>
        </div>
    </div>

    <!-- Main Dashboard -->
    <div id="dashboard" class="container hidden">
        <!-- Sidebar -->
        <div class="sidebar">
            <div class="logo">🛡️ Security Platform</div>
            <div class="nav-item active" data-tab="targets">🎯 Targets</div>
            <div class="nav-item" data-tab="dashboard">📊 Dashboard</div>
            <div class="nav-item" data-tab="scans">🔍 Subdomain Scans</div>
            <div class="nav-item" data-tab="subdomains">🌐 Subdomains</div>
            <div class="nav-item" data-tab="directories">📁 Directories</div>
            <div class="nav-item" data-tab="port-scanning">🔌 Port Scanning</div>
            <div class="nav-item" data-tab="content-discovery">🔍 Content Discovery</div>
            <div class="nav-item" data-tab="js-analysis">📄 JS Analysis</div>
            <div class="nav-item" data-tab="api-discovery">🔗 API Discovery</div>
            <div class="nav-item" data-tab="vuln-scanning">⚠️ Vuln Scanning</div>
            <div class="nav-item" data-tab="settings">⚙️ Settings</div>
            <div class="user-info">
                <div id="user-name" style="color: #00cc00; font-weight: bold;">Loading...</div>
                <div style="margin-top: 12px;">
                    <button onclick="logout()" class="btn btn-secondary btn-small">Logout</button>
                </div>
            </div>
        </div>

        <!-- Main Content -->
        <div class="main">
            <div class="header">
                <h1 id="page-title" style="color: #00ff00;">Targets</h1>
                <div class="header-actions">
                    <select id="global-target-filter" style="padding: 8px 12px; border: 2px solid #003300; background-color: #000000; color: #00ff00;">
                        <option value="">All Targets</option>
                    </select>
                </div>
            </div>
            <div class="content">
                <!-- Targets Tab -->
                <div id="tab-targets" class="tab-content">
                    <div class="card">
                        <div class="card-title">Add New Target</div>
                        <div id="target-messages"></div>
                        <form id="target-form" style="display: grid; grid-template-columns: 1fr 2fr auto; gap: 16px; align-items: end;">
                            <div class="form-group">
                                <label for="target-domain">Domain</label>
                                <input type="text" id="target-domain" placeholder="example.com" required>
                            </div>
                            <div class="form-group">
                                <label for="target-description">Description</label>
                                <input type="text" id="target-description" placeholder="Target description">
                            </div>
                            <button type="submit" class="btn btn-primary" id="add-target-btn">Add Target</button>
                        </form>
                    </div>

                    <div class="card">
                        <div class="card-title">Your Targets</div>
                        <div class="table-container">
                            <table class="table">
                                <thead>
                                    <tr>
                                        <th>Domain</th>
                                        <th>Description</th>
                                        <th>Subdomains</th>
                                        <th>Vulnerabilities</th>
                                        <th>Created</th>
                                        <th>Actions</th>
                                    </tr>
                                </thead>
                                <tbody id="targets-list">
                                    <tr>
                                        <td colspan="6" style="text-align: center; color: #006600;">Loading targets...</td>
                                    </tr>
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>

                <!-- Dashboard Tab -->
                <div id="tab-dashboard" class="tab-content hidden">
                    <div class="stats-grid">
                        <div class="stat-card">
                            <div class="stat-value" style="color: #00ff00;" id="total-targets">0</div>
                            <div class="stat-label">Total Targets</div>
                        </div>
                        <div class="stat-card">
                            <div class="stat-value" style="color: #00ff00;" id="total-subdomains">0</div>
                            <div class="stat-label">Total Subdomains</div>
                        </div>
                        <div class="stat-card">
                            <div class="stat-value" style="color: #ffff00;" id="total-directories">0</div>
                            <div class="stat-label">Total Directories</div>
                        </div>
                        <div class="stat-card">
                            <div class="stat-value" style="color: #ff0000;" id="total-vulnerabilities">0</div>
                            <div class="stat-label">Total Vulnerabilities</div>
                        </div>
                    </div>
                    
                    <div class="card">
                        <div class="card-title">Target Overview</div>
                        <div id="target-overview">
                            <p style="color: #006600;">Loading target overview...</p>
                        </div>
                    </div>
                    
                    <div class="card">
                        <div class="card-title">Recent Activity</div>
                        <div id="recent-activity">
                            <p style="color: #006600;">Loading recent activity...</p>
                        </div>
                    </div>
                </div>

                <!-- Subdomain Scans Tab -->
                <div id="tab-scans" class="tab-content hidden">
                    <div class="scan-info">
                        <h4>🔍 Subdomain Enumeration</h4>
                        <p>Discover subdomains using tools like subfinder and basic DNS enumeration. This is the first step in reconnaissance to map your target's attack surface.</p>
                    </div>

                    <div class="card">
                        <div class="card-title">Start Subdomain Scan</div>
                        <div id="scan-messages"></div>
                        <form id="scan-form">
                            <div style="display: grid; grid-template-columns: 1fr 1fr auto; gap: 20px; align-items: end;">
                                <div class="form-group">
                                    <label for="scan-target">Target Domain</label>
                                    <select id="scan-target" required>
                                        <option value="">Select target...</option>
                                    </select>
                                </div>
                                <div class="form-group">
                                    <label for="scan-priority">Priority</label>
                                    <select id="scan-priority">
                                        <option value="low">Low</option>
                                        <option value="medium" selected>Medium</option>
                                        <option value="high">High</option>
                                    </select>
                                </div>
                                <button type="submit" class="btn btn-primary" id="start-scan-btn">🚀 Start Scan</button>
                            </div>
                        </form>
                    </div>

                    <div class="card">
                        <div class="card-title">Scan Jobs</div>
                        <button onclick="loadScans()" class="btn btn-secondary mb-4">🔄 Refresh</button>
                        <div class="table-container">
                            <table class="table">
                                <thead>
                                    <tr>
                                        <th>ID</th>
                                        <th>Target</th>
                                        <th>Type</th>
                                        <th>Status</th>
                                        <th>Progress</th>
                                        <th>Created</th>
                                        <th>Actions</th>
                                    </tr>
                                </thead>
                                <tbody id="scans-list">
                                    <tr>
                                        <td colspan="7" style="text-align: center; color: #006600;">Loading scans...</td>
                                    </tr>
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>

                <!-- Subdomains Tab -->
                <div id="tab-subdomains" class="tab-content hidden">
                    <div class="filters">
                        <div class="filter-group">
                            <label>Target</label>
                            <select id="subdomain-target-filter">
                                <option value="">All Targets</option>
                            </select>
                        </div>
                        <div class="filter-group">
                            <label>Status</label>
                            <select id="subdomain-status-filter">
                                <option value="">All Status</option>
                                <option value="active">Active</option>
                                <option value="inactive">Inactive</option>
                            </select>
                        </div>
                        <div class="filter-group">
                            <label>Search</label>
                            <input type="text" id="subdomain-search" placeholder="Search subdomains...">
                        </div>
                        <button onclick="loadSubdomains()" class="btn btn-primary">🔄 Refresh</button>
                    </div>

                    <div class="card">
                        <div class="card-title">Discovered Subdomains</div>
                        <div class="table-container">
                            <table class="table">
                                <thead>
                                    <tr>
                                        <th>Subdomain</th>
                                        <th>Target</th>
                                        <th>Status</th>
                                        <th>HTTP Status</th>
                                        <th>IP Address</th>
                                        <th>Title</th>
                                        <th>Actions</th>
                                    </tr>
                                </thead>
                                <tbody id="subdomains-list">
                                    <tr>
                                        <td colspan="7" style="text-align: center; color: #006600;">Loading subdomains...</td>
                                    </tr>
                                </tbody>
                            </table>
                        </div>
                        <div id="subdomains-pagination" class="pagination"></div>
                    </div>
                </div>

                <!-- Directories Tab -->
                <div id="tab-directories" class="tab-content hidden">
                    <div class="filters">
                        <div class="filter-group">
                            <label>Target</label>
                            <select id="directory-target-filter">
                                <option value="">All Targets</option>
                            </select>
                        </div>
                        <div class="filter-group">
                            <label>Subdomain</label>
                            <select id="directory-subdomain-filter">
                                <option value="">All Subdomains</option>
                            </select>
                        </div>
                        <div class="filter-group">
                            <label>Status Code</label>
                            <select id="directory-status-filter">
                                <option value="">All Status</option>
                                <option value="200">200 OK</option>
                                <option value="403">403 Forbidden</option>
                                <option value="404">404 Not Found</option>
                                <option value="500">500 Error</option>
                            </select>
                        </div>
                        <div class="filter-group">
                            <label>Search</label>
                            <input type="text" id="directory-search" placeholder="Search paths...">
                        </div>
                        <button onclick="loadDirectories()" class="btn btn-primary">🔄 Refresh</button>
                    </div>

                    <div class="card">
                        <div class="card-title">Directories & Files</div>
                        <div class="table-container">
                            <table class="table">
                                <thead>
                                    <tr>
                                        <th>Path</th>
                                        <th>Subdomain</th>
                                        <th>Status</th>
                                        <th>Size</th>
                                        <th>Title</th>
                                        <th>Actions</th>
                                    </tr>
                                </thead>
                                <tbody id="directories-list">
                                    <tr>
                                        <td colspan="6" style="text-align: center; color: #006600;">Loading directories...</td>
                                    </tr>
                                </tbody>
                            </table>
                        </div>
                        <div id="directories-pagination" class="pagination"></div>
                    </div>
                </div>

                <!-- Port Scanning Tab -->
                <div id="tab-port-scanning" class="tab-content hidden">
                    <div class="scan-info">
                        <h4>🔌 Port Scanning</h4>
                        <p>Scan for open ports on discovered subdomains using nmap. Identifies services running on different ports to expand the attack surface.</p>
                    </div>

                    <div class="card">
                        <div class="card-title">Start Port Scan</div>
                        <div id="port-scan-messages"></div>
                        <form id="port-scan-form">
                            <div style="display: grid; grid-template-columns: 1fr 1fr 1fr auto; gap: 16px; align-items: end;">
                                <div class="form-group">
                                    <label>Target</label>
                                    <select id="port-scan-target" required>
                                        <option value="">Select target...</option>
                                    </select>
                                </div>
                                <div class="form-group">
                                    <label>Subdomain (optional)</label>
                                    <select id="port-scan-subdomain">
                                        <option value="">All subdomains</option>
                                    </select>
                                </div>
                                <div class="form-group">
                                    <label>Scan Type</label>
                                    <select id="port-scan-type">
                                        <option value="top-1000">Top 1000 Ports</option>
                                        <option value="all">All Ports (Slow)</option>
                                        <option value="common">Common Ports</option>
                                    </select>
                                </div>
                                <button type="submit" class="btn btn-primary">🔌 Start Port Scan</button>
                            </div>
                        </form>
                    </div>

                    <div class="card">
                        <div class="card-title">Open Ports</div>
                        <div class="table-container">
                            <table class="table">
                                <thead>
                                    <tr>
                                        <th>Subdomain</th>
                                        <th>Port</th>
                                        <th>Protocol</th>
                                        <th>Service</th>
                                        <th>Version</th>
                                        <th>State</th>
                                        <th>Actions</th>
                                    </tr>
                                </thead>
                                <tbody id="ports-list">
                                    <tr>
                                        <td colspan="7" style="text-align: center; color: #006600;">Run a port scan to discover open ports</td>
                                    </tr>
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>

                <!-- Content Discovery Tab -->
                <div id="tab-content-discovery" class="tab-content hidden">
                    <div class="scan-info">
                        <h4>🔍 Content Discovery</h4>
                        <p>Discover hidden files, directories, and endpoints using ffuf and wordlists. Finds admin panels, backup files, and other interesting resources.</p>
                    </div>

                    <div class="card">
                        <div class="card-title">Start Content Discovery</div>
                        <div id="content-discovery-messages"></div>
                        <form id="content-discovery-form">
                            <div style="display: grid; grid-template-columns: 1fr 1fr 1fr auto; gap: 16px; align-items: end;">
                                <div class="form-group">
                                    <label>Target</label>
                                    <select id="content-discovery-target" required>
                                        <option value="">Select target...</option>
                                    </select>
                                </div>
                                <div class="form-group">
                                    <label>Subdomain</label>
                                    <select id="content-discovery-subdomain" required>
                                        <option value="">Select subdomain...</option>
                                    </select>
                                </div>
                                <div class="form-group">
                                    <label>Wordlist</label>
                                    <select id="content-discovery-wordlist">
                                        <option value="common">Common Paths</option>
                                        <option value="big">Big Wordlist</option>
                                        <option value="medium">Medium Wordlist</option>
                                    </select>
                                </div>
                                <button type="submit" class="btn btn-primary">🔍 Start Discovery</button>
                            </div>
                        </form>
                    </div>

                    <div class="card">
                        <div class="card-title">Discovered Content</div>
                        <div class="table-container">
                            <table class="table">
                                <thead>
                                    <tr>
                                        <th>URL</th>
                                        <th>Status</th>
                                        <th>Size</th>
                                        <th>Response Time</th>
                                        <th>Title</th>
                                        <th>Actions</th>
                                    </tr>
                                </thead>
                                <tbody id="content-list">
                                    <tr>
                                        <td colspan="6" style="text-align: center; color: #006600;">Run content discovery to find hidden files and directories</td>
                                    </tr>
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>

                <!-- JS Analysis Tab -->
                <div id="tab-js-analysis" class="tab-content hidden">
                    <div class="scan-info">
                        <h4>📄 JavaScript Analysis</h4>
                        <p>Analyze JavaScript files to extract API endpoints, secrets, and sensitive information. Helps identify client-side vulnerabilities and information disclosure.</p>
                    </div>

                    <div class="card">
                        <div class="card-title">Start JS Analysis</div>
                        <div id="js-analysis-messages"></div>
                        <form id="js-analysis-form">
                            <div style="display: grid; grid-template-columns: 1fr 1fr 1fr auto; gap: 16px; align-items: end;">
                                <div class="form-group">
                                    <label>Target</label>
                                    <select id="js-analysis-target" required>
                                        <option value="">Select target...</option>
                                    </select>
                                </div>
                                <div class="form-group">
                                    <label>Subdomain</label>
                                    <select id="js-analysis-subdomain" required>
                                        <option value="">Select subdomain...</option>
                                    </select>
                                </div>
                                <div class="form-group">
                                    <label>Analysis Type</label>
                                    <select id="js-analysis-type">
                                        <option value="endpoints">Extract Endpoints</option>
                                        <option value="secrets">Find Secrets</option>
                                        <option value="both">Both</option>
                                    </select>
                                </div>
                                <button type="submit" class="btn btn-primary">📄 Start Analysis</button>
                            </div>
                        </form>
                    </div>

                    <div class="card">
                        <div class="card-title">JavaScript Files & Findings</div>
                        <div class="table-container">
                            <table class="table">
                                <thead>
                                    <tr>
                                        <th>JS File</th>
                                        <th>Size</th>
                                        <th>Endpoints Found</th>
                                        <th>Secrets Found</th>
                                        <th>Risk Level</th>
                                        <th>Actions</th>
                                    </tr>
                                </thead>
                                <tbody id="js-files-list">
                                    <tr>
                                        <td colspan="6" style="text-align: center; color: #006600;">Run JS analysis to discover JavaScript files and extract secrets</td>
                                    </tr>
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>

                <!-- API Discovery Tab -->
                <div id="tab-api-discovery" class="tab-content hidden">
                    <div class="scan-info">
                        <h4>🔗 API Discovery</h4>
                        <p>Discover REST APIs, GraphQL endpoints, and API documentation. Identifies API endpoints that may be vulnerable to attacks like IDOR, injection, etc.</p>
                    </div>

                    <div class="card">
                        <div class="card-title">Start API Discovery</div>
                        <div id="api-discovery-messages"></div>
                        <form id="api-discovery-form">
                            <div style="display: grid; grid-template-columns: 1fr 1fr 1fr auto; gap: 16px; align-items: end;">
                                <div class="form-group">
                                    <label>Target</label>
                                    <select id="api-discovery-target" required>
                                        <option value="">Select target...</option>
                                    </select>
                                </div>
                                <div class="form-group">
                                    <label>Subdomain</label>
                                    <select id="api-discovery-subdomain" required>
                                        <option value="">Select subdomain...</option>
                                    </select>
                                </div>
                                <div class="form-group">
                                    <label>API Type</label>
                                    <select id="api-discovery-type">
                                        <option value="rest">REST APIs</option>
                                        <option value="graphql">GraphQL</option>
                                        <option value="both">Both</option>
                                    </select>
                                </div>
                                <button type="submit" class="btn btn-primary">🔗 Start Discovery</button>
                            </div>
                        </form>
                    </div>

                    <div class="card">
                        <div class="card-title">API Endpoints</div>
                        <div class="table-container">
                            <table class="table">
                                <thead>
                                    <tr>
                                        <th>Endpoint</th>
                                        <th>Method</th>
                                        <th>Type</th>
                                        <th>Status</th>
                                        <th>Auth Required</th>
                                        <th>Actions</th>
                                    </tr>
                                </thead>
                                <tbody id="api-endpoints-list">
                                    <tr>
                                        <td colspan="6" style="text-align: center; color: #006600;">Run API discovery to find REST and GraphQL endpoints</td>
                                    </tr>
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>

                <!-- Vulnerability Scanning Tab -->
                <div id="tab-vuln-scanning" class="tab-content hidden">
                    <div class="scan-info">
                        <h4>⚠️ Vulnerability Scanning</h4>
                        <p>Run automated vulnerability scans using tools like nuclei. Identifies common security issues like OWASP Top 10 vulnerabilities.</p>
                    </div>

                    <div class="card">
                        <div class="card-title">Start Vulnerability Scan</div>
                        <div id="vuln-scan-messages"></div>
                        <form id="vuln-scan-form">
                            <div style="display: grid; grid-template-columns: 1fr 1fr 1fr auto; gap: 16px; align-items: end;">
                                <div class="form-group">
                                    <label>Target</label>
                                    <select id="vuln-scan-target" required>
                                        <option value="">Select target...</option>
                                    </select>
                                </div>
                                <div class="form-group">
                                    <label>Subdomain</label>
                                    <select id="vuln-scan-subdomain">
                                        <option value="">All subdomains</option>
                                    </select>
                                </div>
                                <div class="form-group">
                                    <label>Scan Profile</label>
                                    <select id="vuln-scan-profile">
                                        <option value="basic">Basic Security Headers</option>
                                        <option value="owasp">OWASP Top 10</option>
                                        <option value="comprehensive">Comprehensive</option>
                                    </select>
                                </div>
                                <button type="submit" class="btn btn-primary">⚠️ Start Vuln Scan</button>
                            </div>
                        </form>
                    </div>

                    <div class="card">
                        <div class="card-title">Vulnerabilities</div>
                        <div class="table-container">
                            <table class="table">
                                <thead>
                                    <tr>
                                        <th>Title</th>
                                        <th>Target</th>
                                        <th>Severity</th>
                                        <th>Status</th>
                                        <th>URL</th>
                                        <th>Found</th>
                                        <th>Actions</th>
                                    </tr>
                                </thead>
                                <tbody id="vulnerabilities-list">
                                    <tr>
                                        <td colspan="7" style="text-align: center; color: #006600;">Loading vulnerabilities...</td>
                                    </tr>
                                </tbody>
                            </table>
                        </div>
                        <div id="vulnerabilities-pagination" class="pagination"></div>
                    </div>
                </div>

                <!-- Settings Tab -->
                <div id="tab-settings" class="tab-content hidden">
                    <div class="card">
                        <div class="card-title">Settings</div>
                        <div class="form-group">
                            <label>API Endpoint</label>
                            <input type="text" id="api-endpoint" value="http://localhost:3001/api/v1" readonly>
                        </div>
                        <div class="form-group">
                            <label>Theme</label>
                            <select>
                                <option>Hacker Matrix (Current)</option>
                            </select>
                        </div>
                        <button class="btn btn-primary" onclick="showMessage('Settings saved!', 'success')">Save Settings</button>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script>
        const API_BASE = 'http://localhost:3001/api/v1';
        let currentUser = null;
        let isRegistering = false;
        let currentPageData = {
            subdomains: { page: 1, total: 0 },
            directories: { page: 1, total: 0 },
            vulnerabilities: { page: 1, total: 0 }
        };

        // Utility functions
        function showMessage(message, type = 'info', containerId = 'auth-messages') {
            const container = document.getElementById(containerId);
            if (!container) return;
            
            const messageEl = document.createElement('div');
            messageEl.className = `message message-${type}`;
            messageEl.textContent = message;
            
            container.innerHTML = '';
            container.appendChild(messageEl);
            
            setTimeout(() => {
                if (messageEl.parentNode) {
                    messageEl.parentNode.removeChild(messageEl);
                }
            }, 5000);
        }

        function setButtonLoading(buttonId, loading = true) {
            const button = document.getElementById(buttonId);
            if (!button) return;
            
            if (loading) {
                button.disabled = true;
                button.innerHTML = '<span class="spinner"></span>Loading...';
            } else {
                button.disabled = false;
                // Restore original text based on button ID
                const originalTexts = {
                    'auth-submit-btn': isRegistering ? 'Register' : 'Login',
                    'add-target-btn': 'Add Target',
                    'start-scan-btn': '🚀 Start Scan'
                };
                button.innerHTML = originalTexts[buttonId] || 'Submit';
            }
        }

        function safeJsonParse(value, fallback = {}) {
            if (!value) return fallback;
            if (typeof value === 'object') return value;
            if (typeof value === 'string') {
                try {
                    return JSON.parse(value);
                } catch (error) {
                    console.warn('JSON parse error for value:', value, error.message);
                    return fallback;
                }
            }
            return fallback;
        }

        // Authentication
        document.getElementById('auth-form').addEventListener('submit', async (e) => {
            e.preventDefault();
            setButtonLoading('auth-submit-btn', true);
            
            const email = document.getElementById('email').value;
            const password = document.getElementById('password').value;
            
            try {
                if (isRegistering) {
                    const fullName = document.getElementById('fullName').value;
                    const organizationName = document.getElementById('organizationName').value;
                    await register(email, password, fullName, organizationName);
                } else {
                    await login(email, password);
                }
            } finally {
                setButtonLoading('auth-submit-btn', false);
            }
        });

        document.getElementById('toggle-auth').addEventListener('click', () => {
            const registerFields = document.getElementById('register-fields');
            const submitBtn = document.getElementById('auth-submit-btn');
            const toggleBtn = document.getElementById('toggle-auth');
            
            if (isRegistering) {
                registerFields.classList.add('hidden');
                submitBtn.textContent = 'Login';
                toggleBtn.textContent = 'Register';
                isRegistering = false;
            } else {
                registerFields.classList.remove('hidden');
                submitBtn.textContent = 'Register';
                toggleBtn.textContent = 'Login';
                isRegistering = true;
            }
        });

        async function login(email, password) {
            try {
                const response = await fetch(`${API_BASE}/auth/login`, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({ email, password })
                });
                
                const data = await response.json();
                
                if (response.ok && data.user) {
                    localStorage.setItem('token', data.accessToken);
                    currentUser = data.user;
                    showDashboard();
                } else {
                    showMessage('Login failed: ' + (data.error || data.message || 'Unknown error'), 'error');
                }
            } catch (error) {
                console.error('Login error:', error);
                showMessage('Login failed: ' + error.message, 'error');
            }
        }

        async function register(email, password, fullName, organizationName) {
            try {
                const response = await fetch(`${API_BASE}/auth/register`, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({ email, password, fullName, organizationName })
                });
                
                const data = await response.json();
                
                if (response.ok && data.user) {
                    localStorage.setItem('token', data.accessToken);
                    currentUser = data.user;
                    showDashboard();
                } else {
                    showMessage('Registration failed: ' + (data.error || data.message || 'Unknown error'), 'error');
                }
            } catch (error) {
                console.error('Registration error:', error);
                showMessage('Registration failed: ' + error.message, 'error');
            }
        }

        function logout() {
            localStorage.removeItem('token');
            currentUser = null;
            document.getElementById('auth-container').classList.remove('hidden');
            document.getElementById('dashboard').classList.add('hidden');
        }

        function showDashboard() {
            document.getElementById('auth-container').classList.add('hidden');
            document.getElementById('dashboard').classList.remove('hidden');
            document.getElementById('user-name').textContent = currentUser.fullName || currentUser.email;
            loadTargets(); // Load targets by default since it's the first tab
        }

        // API helper
        async function apiCall(endpoint, options = {}) {
            const token = localStorage.getItem('token');
            
            try {
                const response = await fetch(`${API_BASE}${endpoint}`, {
                    headers: {
                        'Authorization': `Bearer ${token}`,
                        'Content-Type': 'application/json',
                        ...options.headers
                    },
                    ...options
                });
                
                if (response.status === 401) {
                    // Token expired, redirect to login
                    logout();
                    return null;
                }
                
                return response;
            } catch (error) {
                console.error('API call failed:', error);
                throw error;
            }
        }

        // Navigation
        document.addEventListener('click', (e) => {
            if (e.target.classList.contains('nav-item')) {
                const tab = e.target.getAttribute('data-tab');
                switchTab(tab);
            }
        });

        function switchTab(tab) {
            // Update navigation
            document.querySelectorAll('.nav-item').forEach(item => {
                item.classList.remove('active');
            });
            document.querySelector(`[data-tab="${tab}"]`).classList.add('active');

            // Update content
            document.querySelectorAll('.tab-content').forEach(content => {
                content.classList.add('hidden');
            });
            document.getElementById(`tab-${tab}`).classList.remove('hidden');

            // Update title
            const titles = {
                targets: 'Targets',
                dashboard: 'Dashboard', 
                scans: 'Subdomain Scans',
                subdomains: 'Subdomains',
                directories: 'Directories',
                'port-scanning': 'Port Scanning',
                'content-discovery': 'Content Discovery',
                'js-analysis': 'JS Analysis',
                'api-discovery': 'API Discovery',
                'vuln-scanning': 'Vulnerability Scanning',
                settings: 'Settings'
            };
            document.getElementById('page-title').textContent = titles[tab];

            // Load data for the specific tab
            switch(tab) {
                case 'targets':
                    loadTargets();
                    break;
                case 'dashboard':
                    loadDashboardData();
                    break;
                case 'subdomains':
                    loadSubdomains();
                    break;
                case 'directories':
                    loadDirectories();
                    break;
                case 'vuln-scanning':
                    loadVulnerabilities();
                    break;
                case 'scans':
                    loadScans();
                    break;
                case 'port-scanning':
                    loadPorts();
                    break;
                case 'content-discovery':
                    loadContentDiscovery();
                    break;
                case 'js-analysis':
                    loadJSAnalysis();
                    break;
                case 'api-discovery':
                    loadAPIDiscovery();
                    break;
            }
        }

        // Targets (now first tab)
        async function loadTargets() {
            try {
                const response = await apiCall('/targets');
                if (!response) return;
                
                const data = await response.json();
                const targets = data.success ? data.data : [];
                
                // Update targets list
                const targetsList = document.getElementById('targets-list');
                if (targets.length > 0) {
                    targetsList.innerHTML = targets.map(target => {
                        const stats = safeJsonParse(target.stats, {});
                        return `
                            <tr>
                                <td style="font-weight: 600; color: #00ff00;">${target.domain}</td>
                                <td>${target.description || '-'}</td>
                                <td>${stats.subdomains || 0}</td>
                                <td>
                                    <span class="status ${stats.vulnerabilities > 0 ? 'severity-high' : 'status-inactive'}" style="padding: 4px 8px;">
                                        ${stats.vulnerabilities || 0}
                                    </span>
                                </td>
                                <td>${new Date(target.created_at).toLocaleDateString()}</td>
                                <td>
                                    <button onclick="deleteTarget(${target.id})" class="btn btn-danger btn-small">Delete</button>
                                </td>
                            </tr>
                        `;
                    }).join('');
                } else {
                    targetsList.innerHTML = '<tr><td colspan="6" style="text-align: center; color: #006600;">No targets yet. Add your first target above!</td></tr>';
                }

                // Update all target dropdowns
                updateTargetDropdowns(targets);
                    
            } catch (error) {
                console.error('Failed to load targets:', error);
                document.getElementById('targets-list').innerHTML = '<tr><td colspan="6" style="text-align: center; color: #ff0000;">Failed to load targets</td></tr>';
            }
        }

        function updateTargetDropdowns(targets) {
            const dropdowns = [
                'scan-target',
                'global-target-filter',
                'subdomain-target-filter',
                'directory-target-filter',
                'vuln-scan-target',
                'port-scan-target',
                'content-discovery-target',
                'js-analysis-target',
                'api-discovery-target'
            ];

            dropdowns.forEach(dropdownId => {
                const dropdown = document.getElementById(dropdownId);
                if (dropdown) {
                    const currentValue = dropdown.value;
                    const isFilterDropdown = dropdownId.includes('filter');
                    
                    dropdown.innerHTML = (isFilterDropdown ? '<option value="">All Targets</option>' : '<option value="">Select target...</option>') +
                        targets.map(target => `<option value="${target.id}">${target.domain}</option>`).join('');
                    
                    // Restore previous selection if it still exists
                    if (currentValue && targets.find(t => t.id == currentValue)) {
                        dropdown.value = currentValue;
                    }
                }
            });
        }

        // Target form submission
        document.getElementById('target-form').addEventListener('submit', async (e) => {
            e.preventDefault();
            setButtonLoading('add-target-btn', true);
            
            const domain = document.getElementById('target-domain').value;
            const description = document.getElementById('target-description').value;
            
            try {
                const response = await apiCall('/targets', {
                    method: 'POST',
                    body: JSON.stringify({ domain, description })
                });
                
                if (response && response.ok) {
                    document.getElementById('target-form').reset();
                    loadTargets();
                    showMessage('Target added successfully!', 'success', 'target-messages');
                } else {
                    const errorData = await response.json();
                    showMessage('Failed to add target: ' + (errorData.error || errorData.message || 'Unknown error'), 'error', 'target-messages');
                }
            } catch (error) {
                showMessage('Failed to add target: ' + error.message, 'error', 'target-messages');
            } finally {
                setButtonLoading('add-target-btn', false);
            }
        });

        async function deleteTarget(id) {
            if (confirm('Are you sure you want to delete this target?')) {
                try {
                    const response = await apiCall(`/targets/${id}`, { method: 'DELETE' });
                    if (response && response.ok) {
                        loadTargets();
                        showMessage('Target deleted successfully!', 'success', 'target-messages');
                    } else {
                        showMessage('Failed to delete target', 'error', 'target-messages');
                    }
                } catch (error) {
                    showMessage('Failed to delete target: ' + error.message, 'error', 'target-messages');
                }
            }
        }

        // Load dashboard data
        async function loadDashboardData() {
            try {
                await Promise.all([
                    loadTargets(),
                    loadDashboardStats(),
                    updateGlobalTargetFilter()
                ]);
            } catch (error) {
                console.error('Failed to load dashboard data:', error);
            }
        }

        async function loadDashboardStats() {
            try {
                const targetsResponse = await apiCall('/targets');
                if (!targetsResponse) return;
                
                const targetsData = await targetsResponse.json();
                const targets = targetsData.success ? targetsData.data : [];
                
                // Update basic stats
                document.getElementById('total-targets').textContent = targets.length;
                
                // Get detailed stats for each target
                let totalSubdomains = 0;
                let totalDirectories = 0; 
                let totalVulnerabilities = 0;

                const targetOverviewHtml = targets.map(target => {
                    const targetStats = safeJsonParse(target.stats, {});
                    totalSubdomains += targetStats.subdomains || 0;
                    totalDirectories += targetStats.discovered_paths || 0;
                    totalVulnerabilities += targetStats.vulnerabilities || 0;

                    return `
                        <div class="target-stat" style="background-color: #000000; border: 2px solid #003300; padding: 15px;">
                            <div style="font-weight: bold; color: #00ff00; margin-bottom: 8px;">${target.domain}</div>
                            <div style="display: grid; grid-template-columns: repeat(3, 1fr); gap: 12px;">
                                <div style="text-align: center;">
                                    <div style="font-size: 22px; font-weight: bold; color: #00ff00;">${targetStats.subdomains || 0}</div>
                                    <div style="font-size: 14px; color: #006600;">Subdomains</div>
                                </div>
                                <div style="text-align: center;">
                                    <div style="font-size: 22px; font-weight: bold; color: #ffff00;">${targetStats.discovered_paths || 0}</div>
                                    <div style="font-size: 14px; color: #006600;">Directories</div>
                                </div>
                                <div style="text-align: center;">
                                    <div style="font-size: 22px; font-weight: bold; color: #ff0000;">${targetStats.vulnerabilities || 0}</div>
                                    <div style="font-size: 14px; color: #006600;">Vulnerabilities</div>
                                </div>
                            </div>
                        </div>
                    `;
                }).join('');

                document.getElementById('total-subdomains').textContent = totalSubdomains;
                document.getElementById('total-directories').textContent = totalDirectories;
                document.getElementById('total-vulnerabilities').textContent = totalVulnerabilities;

                if (targetOverviewHtml) {
                    document.getElementById('target-overview').innerHTML = 
                        '<div style="display: grid; gap: 20px;">' + targetOverviewHtml + '</div>';
                } else {
                    document.getElementById('target-overview').innerHTML = 
                        '<p style="color: #006600;">No targets configured yet. Add a target to get started!</p>';
                }

                // Load recent activity
                try {
                    const scansResponse = await apiCall('/scans/jobs?limit=5');
                    if (scansResponse && scansResponse.ok) {
                        const scansData = await scansResponse.json();
                        const recentScans = scansData.success ? scansData.data : [];
                        
                        const recentActivity = document.getElementById('recent-activity');
                        if (recentScans.length > 0) {
                            recentActivity.innerHTML = recentScans.map(scan => 
                                `<div style="padding: 12px 0; border-bottom: 1px solid #003300; display: flex; justify-content: space-between; align-items: center;">
                                    <div>
                                        <strong style="color: #00ff00;">${scan.domain || 'Unknown Target'}</strong>
                                        <span style="color: #006600;"> - ${scan.job_type || 'Unknown Scan'}</span>
                                    </div>
                                    <span class="status status-${scan.status}">${scan.status}</span>
                                </div>`
                            ).join('');
                        } else {
                            recentActivity.innerHTML = '<p style="color: #006600;">No recent scan activity</p>';
                        }
                    }
                } catch (error) {
                    document.getElementById('recent-activity').innerHTML = '<p style="color: #006600;">Recent activity unavailable</p>';
                }
                
            } catch (error) {
                console.error('Failed to load dashboard stats:', error);
                showMessage('Failed to load dashboard statistics', 'error');
            }
        }

        async function updateGlobalTargetFilter() {
            try {
                const response = await apiCall('/targets');
                if (!response) return;
                
                const data = await response.json();
                const targets = data.success ? data.data : [];
                updateTargetDropdowns(targets);
            } catch (error) {
                console.error('Failed to update target filter:', error);
            }
        }

        // Subdomains
        async function loadSubdomains(page = 1) {
            try {
                const targetId = document.getElementById('subdomain-target-filter').value;
                const status = document.getElementById('subdomain-status-filter').value;
                const search = document.getElementById('subdomain-search').value;
                
                const params = new URLSearchParams({
                    page: page,
                    limit: 25
                });
                
                if (targetId) params.append('target_id', targetId);
                if (status) params.append('status', status);
                if (search) params.append('search', search);

                const response = await apiCall(`/subdomains?${params}`);
                if (!response) return;
                
                if (!response.ok) {
                    console.error('Failed to fetch subdomains:', response.status);
                    document.getElementById('subdomains-list').innerHTML = 
                        '<tr><td colspan="7" style="text-align: center; color: #ff0000;">Failed to load subdomains - check if backend is running</td></tr>';
                    return;
                }
                
                const data = await response.json();
                
                if (data.success) {
                    const subdomains = data.data;
                    currentPageData.subdomains = { page, total: data.pagination.total };
                    
                    const subdomainsList = document.getElementById('subdomains-list');
                    
                    if (subdomains.length > 0) {
                        subdomainsList.innerHTML = subdomains.map(subdomain => `
                            <tr>
                                <td style="font-weight: 600; color: #00ff00;">${subdomain.subdomain}</td>
                                <td>${subdomain.target_domain}</td>
                                <td><span class="status status-${subdomain.status}">${subdomain.status}</span></td>
                                <td>${subdomain.http_status || '-'}</td>
                                <td>${subdomain.ip_address || '-'}</td>
                                <td>${subdomain.title || '-'}</td>
                                <td>
                                    <button onclick="checkSubdomainLive(${subdomain.id})" class="btn btn-secondary btn-small">Check Live</button>
                                </td>
                            </tr>
                        `).join('');
                        
                        updatePagination('subdomains', data.pagination);
                    } else {
                        subdomainsList.innerHTML = '<tr><td colspan="7" style="text-align: center; color: #006600;">No subdomains found. Run a subdomain scan to discover subdomains!</td></tr>';
                        const paginationEl = document.getElementById('subdomains-pagination');
                        if (paginationEl) paginationEl.innerHTML = '';
                    }
                }
            } catch (error) {
                console.error('Failed to load subdomains:', error);
                document.getElementById('subdomains-list').innerHTML = 
                    '<tr><td colspan="7" style="text-align: center; color: #ff0000;">Error loading subdomains</td></tr>';
            }
        }

        // Directories
        async function loadDirectories(page = 1) {
            try {
                const targetId = document.getElementById('directory-target-filter').value;
                const subdomainId = document.getElementById('directory-subdomain-filter').value;
                const statusCode = document.getElementById('directory-status-filter').value;
                const search = document.getElementById('directory-search').value;
                
                const params = new URLSearchParams({
                    page: page,
                    limit: 25
                });
                
                if (targetId) params.append('target_id', targetId);
                if (subdomainId) params.append('subdomain_id', subdomainId);
                if (statusCode) params.append('status_code', statusCode);
                if (search) params.append('search', search);

                const response = await apiCall(`/directories?${params}`);
                if (!response) return;
                
                const data = await response.json();
                
                if (data.success) {
                    const directories = data.data;
                    currentPageData.directories = { page, total: data.pagination.total };
                    
                    const directoriesList = document.getElementById('directories-list');
                    
                    if (directories.length > 0) {
                        directoriesList.innerHTML = directories.map(directory => `
                            <tr>
                                <td style="font-family: 'Courier New', monospace; color: #00ff00;">${directory.path}</td>
                                <td>${directory.subdomain}</td>
                                <td>
                                    <span class="status ${getStatusColor(directory.status_code)}">${directory.status_code}</span>
                                </td>
                                <td>${formatBytes(directory.content_length)}</td>
                                <td>${directory.title || '-'}</td>
                                <td>
                                    <button onclick="window.open('${directory.url}', '_blank')" class="btn btn-secondary btn-small">Open</button>
                                </td>
                            </tr>
                        `).join('');
                        
                        updatePagination('directories', data.pagination);
                    } else {
                        directoriesList.innerHTML = '<tr><td colspan="6" style="text-align: center; color: #006600;">No directories found. Run a content discovery scan to find directories!</td></tr>';
                        document.getElementById('directories-pagination').innerHTML = '';
                    }
                }
            } catch (error) {
                console.error('Failed to load directories:', error);
                document.getElementById('directories-list').innerHTML = 
                    '<tr><td colspan="6" style="text-align: center; color: #ff0000;">Failed to load directories</td></tr>';
            }
        }

        // Vulnerabilities
        async function loadVulnerabilities(page = 1) {
            try {
                const targetId = document.getElementById('vuln-scan-target').value;
                const severity = '';
                const status = '';
                
                const params = new URLSearchParams({
                    page: page,
                    limit: 25
                });
                
                if (targetId) params.append('target_id', targetId);
                if (severity) params.append('severity', severity);
                if (status) params.append('status', status);

                const response = await apiCall(`/vulnerabilities?${params}`);
                if (!response) return;
                
                const data = await response.json();
                
                if (data.success) {
                    const vulnerabilities = data.data;
                    currentPageData.vulnerabilities = { page, total: data.pagination.total };
                    
                    const vulnerabilitiesList = document.getElementById('vulnerabilities-list');
                    
                    if (vulnerabilities.length > 0) {
                        vulnerabilitiesList.innerHTML = vulnerabilities.map(vuln => `
                            <tr>
                                <td style="font-weight: 600;">${vuln.title}</td>
                                <td>${vuln.target_domain}</td>
                                <td><span class="status severity-${vuln.severity}">${vuln.severity.toUpperCase()}</span></td>
                                <td><span class="status status-${vuln.status}">${vuln.status.replace('_', ' ')}</span></td>
                                <td style="font-family: 'Courier New', monospace; font-size: 14px;">
                                    ${vuln.url ? `<a href="${vuln.url}" target="_blank" style="color: #00ff00;">${vuln.url.substring(0, 50)}...</a>` : '-'}
                                </td>
                                <td>${new Date(vuln.created_at).toLocaleDateString()}</td>
                                <td>
                                    <button onclick="viewVulnerability(${vuln.id})" class="btn btn-secondary btn-small">View</button>
                                </td>
                            </tr>
                        `).join('');
                        
                        updatePagination('vulnerabilities', data.pagination);
                    } else {
                        vulnerabilitiesList.innerHTML = '<tr><td colspan="7" style="text-align: center; color: #006600;">No vulnerabilities found. Run a vulnerability scan to discover issues!</td></tr>';
                        document.getElementById('vulnerabilities-pagination').innerHTML = '';
                    }
                }
            } catch (error) {
                console.error('Failed to load vulnerabilities:', error);
                document.getElementById('vulnerabilities-list').innerHTML = 
                    '<tr><td colspan="7" style="text-align: center; color: #ff0000;">Failed to load vulnerabilities</td></tr>';
            }
        }

        // Scans
        async function loadScans() {
            try {
                const response = await apiCall('/scans/jobs');
                if (!response) return;
                
                const data = await response.json();
                const scans = data.success ? data.data : [];
                
                const scansList = document.getElementById('scans-list');
                if (scans.length > 0) {
                    scansList.innerHTML = scans.map(scan => `
                        <tr>
                            <td>${scan.id}</td>
                            <td>${scan.domain || 'Unknown'}</td>
                            <td>${scan.job_type || scan.scanType || 'Unknown'}</td>
                            <td><span class="status status-${scan.status}">${scan.status}</span></td>
                            <td>
                                <div style="background-color: #003300; border: 1px solid #00ff00; height: 8px; width: 100px;">
                                    <div style="background-color: #00ff00; height: 100%; width: ${scan.progress_percentage || 0}%;"></div>
                                </div>
                                <span style="font-size: 13px; color: #006600;">${scan.progress_percentage || 0}%</span>
                            </td>
                            <td>${new Date(scan.created_at).toLocaleDateString()}</td>
                            <td>
                                ${scan.status === 'completed' ? 
                                    `<button onclick="viewScanResults(${scan.id})" class="btn btn-secondary btn-small">View Results</button>` :
                                    scan.status === 'running' ? 
                                    `<button onclick="stopScan(${scan.id})" class="btn btn-danger btn-small">Stop</button>` :
                                    '-'
                                }
                            </td>
                        </tr>
                    `).join('');
                } else {
                    scansList.innerHTML = '<tr><td colspan="7" style="text-align: center; color: #006600;">No scans yet. Start your first scan above!</td></tr>';
                }
            } catch (error) {
                console.error('Failed to load scans:', error);
                document.getElementById('scans-list').innerHTML = '<tr><td colspan="7" style="text-align: center; color: #ff0000;">Failed to load scans</td></tr>';
            }
        }

        // Scan form submission (only for subdomain scanning)
        document.getElementById('scan-form').addEventListener('submit', async (e) => {
            e.preventDefault();
            setButtonLoading('start-scan-btn', true);
            
            const targetId = document.getElementById('scan-target').value;
            const priority = document.getElementById('scan-priority').value;
            
            if (!targetId) {
                showMessage('Please select a target', 'error', 'scan-messages');
                setButtonLoading('start-scan-btn', false);
                return;
            }
            
            try {
                const response = await apiCall('/scans/start', {
                    method: 'POST',
                    body: JSON.stringify({
                        targetId: parseInt(targetId),
                        scanTypes: ['subdomain_scan'], // Only subdomain scanning
                        priority
                    })
                });
                
                if (response && response.ok) {
                    showMessage('Subdomain scan started successfully!', 'success', 'scan-messages');
                    loadScans();
                } else {
                    const errorData = await response.json();
                    showMessage('Failed to start scan: ' + (errorData.error || errorData.message || 'Unknown error'), 'error', 'scan-messages');
                }
            } catch (error) {
                showMessage('Failed to start scan: ' + error.message, 'error', 'scan-messages');
            } finally {
                setButtonLoading('start-scan-btn', false);
            }
        });

        // Additional functions
        async function checkSubdomainLive(id) {
            try {
                const response = await apiCall(`/subdomains/${id}/check-live`, { method: 'POST' });
                if (response && response.ok) {
                    loadSubdomains(currentPageData.subdomains.page);
                    showMessage('Live status check completed!', 'success');
                } else {
                    showMessage('Failed to check live status', 'error');
                }
            } catch (error) {
                showMessage('Failed to check live status: ' + error.message, 'error');
            }
        }

        async function viewVulnerability(id) {
            showMessage(`Vulnerability details for ${id} would be shown here`, 'info');
        }

        async function viewScanResults(scanId) {
            try {
                const response = await apiCall(`/scans/${scanId}`);
                if (!response) return;
                
                const data = await response.json();
                
                if (data.success && data.data) {
                    const results = data.data.results || {};
                    alert('Scan Results:\n\n' + JSON.stringify(results, null, 2));
                } else {
                    showMessage('No results available for this scan.', 'warning');
                }
            } catch (error) {
                showMessage('Failed to load scan results: ' + error.message, 'error');
            }
        }

        async function stopScan(scanId) {
            if (confirm('Are you sure you want to stop this scan?')) {
                try {
                    const response = await apiCall(`/scans/stop/${scanId}`, { method: 'POST' });
                    if (response && response.ok) {
                        loadScans();
                        showMessage('Scan stopped successfully!', 'success');
                    } else {
                        showMessage('Failed to stop scan', 'error');
                    }
                } catch (error) {
                    showMessage('Failed to stop scan: ' + error.message, 'error');
                }
            }
        }

        // Specialized scan functions (placeholders for now)
        async function loadPorts() {
            const portsList = document.getElementById('ports-list');
            if (portsList) {
                portsList.innerHTML = '<tr><td colspan="7" style="text-align: center; color: #006600;">Run a port scan to discover open ports</td></tr>';
            }
        }

        async function loadContentDiscovery() {
            const contentList = document.getElementById('content-list');
            if (contentList) {
                contentList.innerHTML = '<tr><td colspan="6" style="text-align: center; color: #006600;">Run content discovery to find hidden files and directories</td></tr>';
            }
        }

        async function loadJSAnalysis() {
            const jsList = document.getElementById('js-files-list');
            if (jsList) {
                jsList.innerHTML = '<tr><td colspan="6" style="text-align: center; color: #006600;">Run JS analysis to discover JavaScript files and extract secrets</td></tr>';
            }
        }

        async function loadAPIDiscovery() {
            const apiList = document.getElementById('api-endpoints-list');
            if (apiList) {
                apiList.innerHTML = '<tr><td colspan="6" style="text-align: center; color: #006600;">Run API discovery to find REST and GraphQL endpoints</td></tr>';
            }
        }

        // Utility functions
        function updatePagination(type, pagination) {
            const paginationContainer = document.getElementById(`${type}-pagination`);
            if (!paginationContainer) return;
            
            const { page, pages, total } = pagination;
            let paginationHtml = '';
            
            if (pages <= 1) {
                paginationContainer.innerHTML = '';
                return;
            }
            
            paginationHtml += `<button onclick="load${capitalizeFirst(type)}(${page - 1})" ${page <= 1 ? 'disabled' : ''} class="btn btn-secondary btn-small">Previous</button>`;
            
            for (let i = Math.max(1, page - 2); i <= Math.min(pages, page + 2); i++) {
                paginationHtml += `<button onclick="load${capitalizeFirst(type)}(${i})" class="btn ${i === page ? 'btn-primary' : 'btn-secondary'} btn-small">${i}</button>`;
            }
            
            paginationHtml += `<button onclick="load${capitalizeFirst(type)}(${page + 1})" ${page >= pages ? 'disabled' : ''} class="btn btn-secondary btn-small">Next</button>`;
            
            paginationContainer.innerHTML = paginationHtml;
        }

        function capitalizeFirst(str) {
            return str.charAt(0).toUpperCase() + str.slice(1);
        }

        function getStatusColor(statusCode) {
            if (!statusCode) return 'status-inactive';
            if (statusCode >= 200 && statusCode < 300) return 'status-completed';
            if (statusCode >= 300 && statusCode < 400) return 'status-running';
            if (statusCode >= 400 && statusCode < 500) return 'status-pending';
            if (statusCode >= 500) return 'status-failed';
            return 'status-inactive';
        }

        function formatBytes(bytes) {
            if (!bytes || bytes === 0) return '-';
            const k = 1024;
            const sizes = ['B', 'KB', 'MB', 'GB'];
            const i = Math.floor(Math.log(bytes) / Math.log(k));
            return parseFloat((bytes / Math.pow(k, i)).toFixed(1)) + ' ' + sizes[i];
        }

        function debounce(func, wait) {
            let timeout;
            return function executedFunction(...args) {
                const later = () => {
                    clearTimeout(timeout);
                    func(...args);
                };
                clearTimeout(timeout);
                timeout = setTimeout(later, wait);
            };
        }

        // Event listeners
        document.getElementById('global-target-filter').addEventListener('change', (e) => {
            const selectedTargetId = e.target.value;
            
            ['subdomain-target-filter', 'directory-target-filter', 'vuln-scan-target'].forEach(filterId => {
                const filter = document.getElementById(filterId);
                if (filter) {
                    filter.value = selectedTargetId;
                }
            });
            
            const activeTab = document.querySelector('.nav-item.active').dataset.tab;
            switch(activeTab) {
                case 'subdomains':
                    loadSubdomains();
                    break;
                case 'directories':
                    loadDirectories();
                    break;
                case 'vuln-scanning':
                    loadVulnerabilities();
                    break;
            }
        });

        ['subdomain-target-filter', 'subdomain-status-filter'].forEach(filterId => {
            const element = document.getElementById(filterId);
            if (element) {
                element.addEventListener('change', () => loadSubdomains(1));
            }
        });

        ['directory-target-filter', 'directory-subdomain-filter', 'directory-status-filter'].forEach(filterId => {
            const element = document.getElementById(filterId);
            if (element) {
                element.addEventListener('change', () => loadDirectories(1));
            }
        });

        const subdomainSearch = document.getElementById('subdomain-search');
        if (subdomainSearch) {
            subdomainSearch.addEventListener('input', debounce(() => loadSubdomains(1), 500));
        }

        const directorySearch = document.getElementById('directory-search');
        if (directorySearch) {
            directorySearch.addEventListener('input', debounce(() => loadDirectories(1), 500));
        }

        // Check authentication on load
        window.addEventListener('load', async () => {
            const token = localStorage.getItem('token');
            if (token) {
                try {
                    const response = await apiCall('/targets');
                    if (response && response.ok) {
                        document.getElementById('auth-container').classList.add('hidden');
                        document.getElementById('dashboard').classList.remove('hidden');
                        document.getElementById('user-name').textContent = 'Loading...';
                        await loadTargets(); // Load targets since it's now the default tab
                    } else {
                        localStorage.removeItem('token');
                        document.getElementById('auth-container').classList.remove('hidden');
                        document.getElementById('dashboard').classList.add('hidden');
                    }
                } catch (error) {
                    console.error('Token validation failed:', error);
                    localStorage.removeItem('token');
                    document.getElementById('auth-container').classList.remove('hidden');
                    document.getElementById('dashboard').classList.add('hidden');
                }
            } else {
                document.getElementById('auth-container').classList.remove('hidden');
                document.getElementById('dashboard').classList.add('hidden');
            }
        });

        // Auto-refresh for scans
        let refreshInterval = null;

        function startAutoRefresh() {
            if (refreshInterval) return;
            
            refreshInterval = setInterval(async () => {
                const activeTab = document.querySelector('.nav-item.active')?.dataset.tab;
                if (activeTab === 'scans') {
                    try {
                        const response = await apiCall('/scans/jobs');
                        if (response && response.ok) {
                            const data = await response.json();
                            const scans = data.success ? data.data : [];
                            const hasRunningScans = scans.some(scan => scan.status === 'running' || scan.status === 'pending');
                            
                            if (hasRunningScans) {
                                loadScans();
                            } else {
                                stopAutoRefresh();
                            }
                        }
                    } catch (error) {
                        console.error('Auto-refresh failed:', error);
                    }
                }
            }, 5000);
        }

        function stopAutoRefresh() {
            if (refreshInterval) {
                clearInterval(refreshInterval);
                refreshInterval = null;
            }
        }

        document.addEventListener('click', (e) => {
            if (e.target.classList.contains('nav-item') && e.target.getAttribute('data-tab') === 'scans') {
                setTimeout(startAutoRefresh, 1000);
            } else if (e.target.classList.contains('nav-item')) {
                stopAutoRefresh();
            }
        });
    </script>
</body>
</html>